<h1>Lead#Edit</h1>
<%= form_for lead, local: true, class: "form-inline" do |f| %>

  <%= render 'form', lead: @lead, users: @users, f: f %>
  <%= f.submit "Update", class: 'btn btn-success' %>
<%end%>
